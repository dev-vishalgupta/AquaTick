/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  darkMode: "media", // Automatically matches the system theme (media query mode)
  theme: {
    extend: {},
  },
  plugins: [],
};
